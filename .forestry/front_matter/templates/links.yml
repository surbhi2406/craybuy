---
label: links
hide_body: true
fields:
- type: field_group_list
  name: Links
  label: Links
  fields:
  - type: text
    name: name
    label: name
    config:
      required: true
  - type: text
    name: link
    label: link
  - name: links
    type: field_group_list
    fields:
    - name: name
      type: text
      config:
        required: false
      label: name
    - name: link
      type: text
      config:
        required: false
      label: link
    config:
      min: 
      max: 
      labelField: 
    label: links
